// Shared TypeScript interfaces and types

// Placeholder for now - will be populated with actual types later
export interface User {
  id: string;
  // Add more fields as needed
}

export interface Session {
  id: string;
  // Add more fields as needed
}

export interface Message {
  id: string;
  // Add more fields as needed
}

